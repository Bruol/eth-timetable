{
  "name": "eth_timetable",
  "private": true,
  "scripts": {
    "split": "bun run split-ics.ts",
    "split:file": "bun run split-ics.ts --",
    "google-import": "bun run import-to-google-calendar.ts",
    "google-import:config": "bun run import-to-google-calendar.ts --",
    "google-delete": "bun run --bun delete-calendars.ts",
    "google-delete:auto": "bun run --bun delete-calendars.ts -- --auto"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "googleapis": "^171.4.0"
  }
}
